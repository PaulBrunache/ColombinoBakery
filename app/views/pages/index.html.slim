h1 Listing pages

table
  thead
    tr
      th Page Name
      th Picture
      th Default
      th
      th

  tbody
    - @pages.each do |page|
      tr
        td = page.page_name
        td = image_tag(page.picture.url, size: '100x100') if page.picture?
        td = page.active
        td = link_to 'Show', page
        td = link_to 'Edit', edit_page_path(page)
        td = link_to 'Destroy', page, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Page', new_page_path
