nav.fixed
  .mobile
      button.menu-toggle.fa.fa-bars.fa-2x
  -if logged_in?
    ul.menu.bordered.horizontal.right
      li = link_to 'Logout', logout_path, method: "delete" 
      li = link_to 'Admin Home', user_path(current_user)
  -else
    ul.menu.bordered.horizontal
      = active_link 'Home', root_path
      = active_link 'Catering', catering_path
      = active_link 'Deli', deli_path
      = active_link 'Pastries', pastries_path
      = active_link 'pizza', pizza_path
      = active_link 'Subs', subs_path

